<Window xmlns:WindowParts="clr-namespace:Seo.WindowParts"
        x:Class="Seo.DocumentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MultiDocument" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Loaded="Window_Loaded" Icon="icon.ico">
    <Grid Width="300" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="ContentText" Grid.Row="0" />
        <StackPanel x:Name="DocRadioPanel" Grid.Row="1" />
        <StackPanel Grid.Row="2" Margin="0,8,0,0"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <WindowParts:SimpleButton x:Name="OkButton" Click="OkButton_Click"
                                      Margin="4,0" Width="72" Height="24"
                                      Text="OK" />
            <WindowParts:SimpleButton x:Name="CancelButton" Click="CancelButton_Click"
                                      Margin="4,0" Width="72" Height="24"
                                      Text="Cancel" />
        </StackPanel>
    </Grid>
</Window>
